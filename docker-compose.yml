services:
  web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://taltech.akaver.com}
        VITE_API_VERSION: ${VITE_API_VERSION:-1.0}
    container_name: js-assignment-4
    ports:
      - "82:80"
    restart: unless-stopped
